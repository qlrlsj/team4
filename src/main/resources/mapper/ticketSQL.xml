<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ticket">
		<!-- select id="DAO에서 보내준 메소드명과 동일하다" -->
	<!-- parameterType="패키지 경로를 전부 적어주어야 구분이 가능하다 mapper로 들어온 값을 의미한다" (Mybatis에서는 파라미터타입을 한개만 가능하다) -->
	<!-- resultType="mapper에서 나가는 값을 의미한다" -->
	<!-- <select id="selectOneMember" parameterType="kr.or.member.model.vo.Member" resultType="kr.or.member.model.vo.Member"></select> -->
	
	<select id="selectAllTicketCategory" resultType="tc"> <!-- 위와 동일하나, mybatis-config.xml에서 별칭을 지정했다 m으로 사용하기로 -->
		select
			category_id as categoryId, <!-- 쿼리를 이렇게 치는 이유 실제 테이블 컬럼명과 VO 변수명을 동일하게 맞춰주면 Mybatis에서는 자동으로 Member 객체로 묶어준다 -->
			category_name as categoryName,
			parent_category_id as parentCategoryId
		from ticket_category <!-- #을쓰고 {}를 사용하면 파라미터를 사용하겠다는걸로 Mybatis가 인지한다 -->
	</select>
	
</mapper>